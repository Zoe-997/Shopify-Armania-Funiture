<!-- customers/login.liquid  -->
{% section 'breadcrumb' %}
<div class="main-content shop-page login-page">
  <div class="container">
    
    <div class="login-register-form content-form row pr_center ">
      <div class="note form-success hide" id="ResetSuccess" style="display:none;">
        {{ 'customer.recover_password.success' | t }}
      </div>
      <div class="cms-left col-xs-12 col-sm-12 col-md-5 col-lg-5">
        <div class="switch-form" id="loginForm" >
        {% form 'customer_login', class:'login' %}        
        {{ form.errors | default_errors }}
        <div class="login-form">          
          <h4 class="main-title">{{'customer.login.log_in' | t}}</h4>
          
          <div class="box-label">
            <label>{{'customer.login.email' | t}} <span>*</span></label>
            <input
                   class="input-info {% if form.errors contains 'email' %} input--error{% endif %}"	
                   type="email"
                   name="customer[email]"
                   id="CustomerEmail"
                   autocomplete="email"
                   required="required"
                   autocorrect="off"
                   autocapitalize="off"
                   {% if form.errors contains 'form' %}
                   aria-invalid="true"
                   {% endif %}
                   placeholder="{{ 'customer.login.email' | t }}"
                   >
          </div>
            {% if form.password_needed %}      
          	<div class="box-label">
            <label>{{'customer.login.password' |t}} <span>*</span></label>
             <input
                    class="input-info {% if form.errors contains 'password' %} input--error{% endif %}"
                     type="password"
                     value=""
                    required="required"
                     name="customer[password]"
                     id="CustomerPassword"
                     autocomplete="current-password"
                     {% if form.errors contains 'form' %}
                     aria-invalid="true"
                     {% endif %}
                     placeholder="{{ 'customer.login.password' | t }}"
                     >
          </div>
          {% endif %}
         
          <div class="check-box">            
            <input class="login-check" type="checkbox" id="login-check" name="rememberme" value="forever" ><label class="text-label" for="login-check"> Remember me </label>	
            {% if form.password_needed %}
            	<a href="#recoverPassword" class="switch-button recoverPassword forgot">{{ 'customer.login.forgot_password' | t }}</a>
            {% endif %}
          </div>
          <div class="group-button">            
            <input type="submit" class="button submit" name="login" value="{{ 'customer.login.sign_in' | t }}"/>
            
          </div>
          <p class="or"><span>{{'customer.register.or' | t}}</span></p>
          <div class="register_submit">
              <a href="/account/register" >
                {{'customer.register.submit' | t}}
              </a>
            </div>
          
        </div>
        {% endform %}
        </div>
    
          <div id="recoverPassword" class="switch-form hidden">
            <div class="text-center">
              <h4 class="main-title" style="text-align:left;">{{ 'customer.recover_password.title' | t }}</h4>
              <p style="text-align:left;">{{ 'customer.recover_password.subtext' | t }}</p>
            </div>
            <div class="form-vertical">
              {% form 'recover_customer_password' %}

              {{ form.errors | default_errors }}

              {% comment %}
              Add a hidden span to indicate the form was submitted succesfully.
              {% endcomment %}
              {% if form.posted_successfully? %}
              	<span class="hide reset-password-success"></span>
              {% endif %}
			  <div class="box-label">
                
              		<label>{{ 'customer.recover_password.email' | t }}<span class="required">*</span></label>
              		<input  type="email" value="" name="email" placeholder={{'customer.recover_password.placeholder' | t}} id="RecoverEmail" class="input-info" required>
              </div>
              
              <div class="group-button">
                <input  type="submit" class="button submit" value="{{ 'customer.recover_password.submit' | t }}"> 
              </div>
              <a href="#loginForm" class="loginForm switch-button" class="button submit">{{ 'customer.recover_password.cancel' | t }}</a>
              {% endform %}
            </div>
          </div>
          
          {% comment %}
            If accounts are set as optional, the following will be shown as an option
            during checkout, not on the default /login page.
            {% endcomment %}
            {% if shop.checkout.guest_login %}
            <div class="text-center">
              <hr class="hr--invisible">
              <h2>{{ 'customer.login.guest_title' | t }}</h2>

              {% form 'guest_login' %}
              <input type="submit" class="submit button" value="{{ 'customer.login.guest_continue' | t }}">
              {% endform %}
            </div>
            {% endif %}
    
      </div>
      
      {%comment%}
      <div class="cms-right col-xs-12 col-sm-12 col-md-6 col-lg-6">
        {% form 'create_customer', class:'register' %}
        {{ form.errors | default_errors }}
        <div class="register-form">
          <h4 class="main-title">{{ 'customer.register.title' | t }}</h4>
          <div class="row">
            <div class="col-xs-12 col-sm-6 col-md-6 col-lg-6">
              <span class="label-text">{{'customer.register.first_name' | t}} <span>*</span></span>              
              <input type="text" required id="reg_first_name" class="input-info {% if form.errors contains 'first_name' %} input--error{% endif %}" name="customer[first_name]" {% if form.email %} value="{{ form.first_name }}"{% endif %}/>
            </div>     
            <div class="col-xs-12 col-sm-6 col-md-6 col-lg-6">
              <span class="label-text">{{ 'customer.register.middle_name' | t }}</span>
              <input type="text" class="input-info">	
            </div>
          </div>
          <span class="label-text">{{'customer.register.last_name' |t}} <span>*</span></span>
          <input type="text" id="reg_last_name" required class="input-info {% if form.errors contains 'last_name' %} input--error{% endif %}" name="customer[last_name]" {% if form.email %} value="{{ form.last_name }}"{% endif %}/>	
          <span class="label-text">{{'customer.register.email' | t}} <span>*</span></span>          
          <input type="email" id="reg_email" required class="input-info {% if form.errors contains 'email' %} input--error{% endif %}" name="customer[email]" {% if form.email %} value="{{ form.email }}"{% endif %}/>
          
          <h5 class="title">{{'customer.register.sub_title' |t}}</h5>
          <div class="row">
            <div class="col-xs-12 col-sm-6 col-md-6 col-lg-6">
              <span class="label-text">{{'customer.register.password' | t}} *</span>              
              <input type="password" required name="customer[password]" id="createPassword" class="input-info{% if form.errors contains 'password' %} input--error{% endif %}">
            </div>
            <div class="col-xs-12 col-sm-6 col-md-6 col-lg-6">
              <span class="label-text">{{ 'customer.reset_password.password_confirm' | t }} *</span>              
              <input type="password" value="" name="customer[password_confirmation]" id="PasswordConfirmation" class="input-info {% if form.errors contains 'password_confirmation' %} input--error{% endif %}">
            </div>
          </div>
          <div class="group-button">            
            <input type="submit" class="button submit" value="{{ 'customer.register.submit' | t }}"/>
          </div>		
        </div>
        {% endform %}
      </div>
      {%endcomment%}

      <ul class="menu-language">
      <li class="language"  {%if section.settings.header_use_language%} {%else%} style="display:none" {% endif %}>
        <a href="#" class="toggle-submenu"><span class="flag">{{ 'flag1.jpg' | asset_url | img_tag: shop.name }}</span> English</a>        
      </li>
      <li class="currencies menu-item-has-children" {%if section.settings.header_use_currency%} {%else%} style="display:none" {% endif %}>
        {% include 'currency-picker' %} 
        <a href="#" class="toggle-submenu lang-currency"><span class="text">United State</span>(USD)</a>
        <ul class="list-currencies currency-lists">
              {% assign codes = 'USD,EUR,GBP,CAD,ARS,AUD,BBD,BDT,BSD,BHD,BRL,BOB,BND,BGN,ILS,MMK,KYD,CLP,CNY,COP,CRC,HRK,CZK,DKK,DOP,XCD,EGP,XPF,FJD,GHS,GTQ,GYD,GEL,HKD,HUF,ISK,INR,IDR,NIS,JMD,JPY,JOD,KZT,KES,KWD,LVL,LTL,MXN,MYR,MUR,MDL,MAD,MNT,MZN,ANG,NZD,NGN,NOK,OMR,PKR,PYG,PEN,PHP,PLN,QAR,RON,RUB,SAR,RSD,SCR,SGD,SYP,ZAR,KRW,LKR,SEK,CHF,TWD,THB,TZS,TTD,TRY,UAH,AED,UYU,VEB,VND,ZMK' | split: ',' %}
              {% assign supported_codes = settings.supported_currencies | split: ' ' %}                
              {% for code in supported_codes %}
                  {% if codes contains code %}
                      {% case code %} 
                      {% when 'USD' %}
                      <li data-value="{{ code }}" ><a href="javascript:void(0);" class="{{ code }}"><span class="text">United State</span> (<span class="code_currency">{{ code }}</span>)</a></li>
                      {% when 'EUR' %}
                      <li data-value="{{ code }}" ><a href="javascript:void(0);" class="{{ code }}"><span class="text">Euro</span> (<span class="code_currency">{{ code }}</span>)</a></li>
                      {% when 'GBP'  %}
                      <li data-value="{{ code }}" ><a href="javascript:void(0);" class="{{ code }}"><span class="text">England</span> (<span class="code_currency">{{ code }}</span>)</a></li>
                      {% when 'CAD' %}
                      <li data-value="{{ code }}" ><a href="javascript:void(0);" class="{{ code }}"><span class="text">Canada</span> (<span class="code_currency">{{ code }}</span>)</a></li>
                      {% when 'VND' %}
                      <li data-value="{{ code }}" ><a href="javascript:void(0);" class="{{ code }}"><span class="text">Viet Nam</span> (<span class="code_currency">{{ code }}</span>)</a></li>
                      {% when 'JPY' %}
                      <li data-value="{{ code }}" ><a href="javascript:void(0);" class="{{ code }}"><span class="text">Japan</span> (<span class="code_currency">{{ code }}</span>)</a></li>
                      {% when 'AUD' %}
                      <li data-value="{{ code }}" ><a href="javascript:void(0);" class="{{ code }}"><span class="text">Australia</span> (<span class="code_currency">{{ code }}</span>)</a></li>
                      {% when 'CNY' %}
                      <li data-value="{{ code }}" ><a href="javascript:void(0);" class="{{ code }}"><span class="text">China</span> (<span class="code_currency">{{ code }}</span>)</a></li>
                      {% else %}
                      <li data-value="{{ code }}" ><a href="javascript:void(0);" class="{{ code }}">(<span class="code_currency">{{ code }}</span>)</a></li>
                      {% endcase %} 
                  {% endif %}
              {% endfor %} 
        </ul>
      </li>
    </ul>

    <ul class="list-socials">
      {% if settings.social_link_twitter_enable %}
      <li>
        <a class="twitter" target="_blank" href="{{settings.social_link_twitter_link}}" title="{{settings.social_link_twitter_text}}"><i class="fa fa-twitter"></i></a>
      </li>
      {% endif %}
      {% if settings.social_link_facebook_enable %}
      <li>
        <a class="facebook" target="_blank" href="{{settings.social_link_facebook_link}}" title="{{settings.social_link_facebook_text}}"><i class="fa fa-facebook"></i></a>
      <li>
        {% endif %}
        {% if settings.social_link_google_enable %}
      <li>
        <a class="google" target="_blank" href="{{settings.social_link_google_link}}" title="{{settings.social_link_google_text}}"><i class="fa fa-google-plus"></i></a>
      </li>
      {% endif %}
      {% if settings.social_link_pinterest_enable %}
      <li>
        <a class="pinterest" target="_blank" href="{{settings.social_link_pinterest_link}}" title="{{settings.social_link_pinterest_text}}"><i class="fa fa-pinterest-p"></i></a>
      </li>
      {% endif %}
      {% if settings.social_link_instagram_enable %}
      <li>
        <a class="instagram" target="_blank" href="{{settings.social_link_instagram_link}}" title="{{settings.social_link_instagram_text}}"><i class="fa fa-instagram"></i></a>
      </li>
      {% endif %}

      {% if settings.social_link_tumblr_enable %}
      <li>
        <a class="tumblr" target="_blank" href="{{settings.social_tumblr_link}}" title="{{settings.social_link_tumblr_text}}"><i class="fa fa-tumblr-square"></i></a>
      </li>
      {% endif %}
      {% if settings.social_link_snapchat_enable %}
      <li>
        <a class="tumblr" target="_blank" href="{{settings.social_snapchat_link}}" title="{{settings.social_link_snapchat_text}}"><i class="fa fa-snapchat-ghost"></i></a>
      </li>
      {% endif %}
      {% if settings.social_link_youtube_enable %}
      <li>
        <a class="tumblr" target="_blank" href="{{settings.social_youtube_link}}" title="{{settings.social_link_youtube_text}}"><i class="fa fa-youtube-square"></i></a>
      </li>
      {% endif %}
      {% if settings.social_link_vimeo_enable %}
      <a class="tumblr" target="_blank" href="{{settings.social_vimeo_link}}" title="{{settings.social_link_vimeo_text}}"><i class="fa fa-vimeo "></i></a>
      {% endif %} 
    </ul>

    <ul class="top-links">
      {% if customer %}
        <li><a href="/account/logout">{{'layout.customer.log_out'|t}}</a></li>
      {% else %}
        <li><a href="/account/login">{{'layout.customer.log_in' | t}}</a></li>
      {% endif %}    
     
    </ul>

    <ul class="menu-language">
      <li class="language"  {%if section.settings.header_use_language%} {%else%} style="display:none" {% endif %}>
        <a href="#" class="toggle-submenu"><span class="flag">{{ 'flag1.jpg' | asset_url | img_tag: shop.name }}</span> English</a>        
      </li>
      <li class="currencies menu-item-has-children" {%if section.settings.header_use_currency%} {%else%} style="display:none" {% endif %}>
        {% include 'currency-picker' %} 
        <a href="#" class="toggle-submenu lang-currency"><span class="text">United State</span>(USD)</a>
        <ul class="list-currencies currency-lists">
              {% assign codes = 'USD,EUR,GBP,CAD,ARS,AUD,BBD,BDT,BSD,BHD,BRL,BOB,BND,BGN,ILS,MMK,KYD,CLP,CNY,COP,CRC,HRK,CZK,DKK,DOP,XCD,EGP,XPF,FJD,GHS,GTQ,GYD,GEL,HKD,HUF,ISK,INR,IDR,NIS,JMD,JPY,JOD,KZT,KES,KWD,LVL,LTL,MXN,MYR,MUR,MDL,MAD,MNT,MZN,ANG,NZD,NGN,NOK,OMR,PKR,PYG,PEN,PHP,PLN,QAR,RON,RUB,SAR,RSD,SCR,SGD,SYP,ZAR,KRW,LKR,SEK,CHF,TWD,THB,TZS,TTD,TRY,UAH,AED,UYU,VEB,VND,ZMK' | split: ',' %}
              {% assign supported_codes = settings.supported_currencies | split: ' ' %}                
              {% for code in supported_codes %}
                  {% if codes contains code %}
                      {% case code %} 
                      {% when 'USD' %}
                      <li data-value="{{ code }}" ><a href="javascript:void(0);" class="{{ code }}"><span class="text">United State</span> (<span class="code_currency">{{ code }}</span>)</a></li>
                      {% when 'EUR' %}
                      <li data-value="{{ code }}" ><a href="javascript:void(0);" class="{{ code }}"><span class="text">Euro</span> (<span class="code_currency">{{ code }}</span>)</a></li>
                      {% when 'GBP'  %}
                      <li data-value="{{ code }}" ><a href="javascript:void(0);" class="{{ code }}"><span class="text">England</span> (<span class="code_currency">{{ code }}</span>)</a></li>
                      {% when 'CAD' %}
                      <li data-value="{{ code }}" ><a href="javascript:void(0);" class="{{ code }}"><span class="text">Canada</span> (<span class="code_currency">{{ code }}</span>)</a></li>
                      {% when 'VND' %}
                      <li data-value="{{ code }}" ><a href="javascript:void(0);" class="{{ code }}"><span class="text">Viet Nam</span> (<span class="code_currency">{{ code }}</span>)</a></li>
                      {% when 'JPY' %}
                      <li data-value="{{ code }}" ><a href="javascript:void(0);" class="{{ code }}"><span class="text">Japan</span> (<span class="code_currency">{{ code }}</span>)</a></li>
                      {% when 'AUD' %}
                      <li data-value="{{ code }}" ><a href="javascript:void(0);" class="{{ code }}"><span class="text">Australia</span> (<span class="code_currency">{{ code }}</span>)</a></li>
                      {% when 'CNY' %}
                      <li data-value="{{ code }}" ><a href="javascript:void(0);" class="{{ code }}"><span class="text">China</span> (<span class="code_currency">{{ code }}</span>)</a></li>
                      {% else %}
                      <li data-value="{{ code }}" ><a href="javascript:void(0);" class="{{ code }}">(<span class="code_currency">{{ code }}</span>)</a></li>
                      {% endcase %} 
                  {% endif %}
              {% endfor %} 
        </ul>
      </li>
    </ul>

    <ul class="list-socials">
      {% if settings.social_link_twitter_enable %}
      <li>
        <a class="twitter" target="_blank" href="{{settings.social_link_twitter_link}}" title="{{settings.social_link_twitter_text}}"><i class="fa fa-twitter"></i></a>
      </li>
      {% endif %}
      {% if settings.social_link_facebook_enable %}
      <li>
        <a class="facebook" target="_blank" href="{{settings.social_link_facebook_link}}" title="{{settings.social_link_facebook_text}}"><i class="fa fa-facebook"></i></a>
      <li>
        {% endif %}
        {% if settings.social_link_google_enable %}
      <li>
        <a class="google" target="_blank" href="{{settings.social_link_google_link}}" title="{{settings.social_link_google_text}}"><i class="fa fa-google-plus"></i></a>
      </li>
      {% endif %}
      {% if settings.social_link_pinterest_enable %}
      <li>
        <a class="pinterest" target="_blank" href="{{settings.social_link_pinterest_link}}" title="{{settings.social_link_pinterest_text}}"><i class="fa fa-pinterest-p"></i></a>
      </li>
      {% endif %}
      {% if settings.social_link_instagram_enable %}
      <li>
        <a class="instagram" target="_blank" href="{{settings.social_link_instagram_link}}" title="{{settings.social_link_instagram_text}}"><i class="fa fa-instagram"></i></a>
      </li>
      {% endif %}

      {% if settings.social_link_tumblr_enable %}
      <li>
        <a class="tumblr" target="_blank" href="{{settings.social_tumblr_link}}" title="{{settings.social_link_tumblr_text}}"><i class="fa fa-tumblr-square"></i></a>
      </li>
      {% endif %}
      {% if settings.social_link_snapchat_enable %}
      <li>
        <a class="tumblr" target="_blank" href="{{settings.social_snapchat_link}}" title="{{settings.social_link_snapchat_text}}"><i class="fa fa-snapchat-ghost"></i></a>
      </li>
      {% endif %}
      {% if settings.social_link_youtube_enable %}
      <li>
        <a class="tumblr" target="_blank" href="{{settings.social_youtube_link}}" title="{{settings.social_link_youtube_text}}"><i class="fa fa-youtube-square"></i></a>
      </li>
      {% endif %}
      {% if settings.social_link_vimeo_enable %}
      <a class="tumblr" target="_blank" href="{{settings.social_vimeo_link}}" title="{{settings.social_link_vimeo_text}}"><i class="fa fa-vimeo "></i></a>
      {% endif %} 
    </ul>

    <ul class="top-links">
      {% if customer %}
        <li><a href="/account/logout">{{'layout.customer.log_out'|t}}</a></li>
      {% else %}
        <li><a href="/account/login">{{'layout.customer.log_in' | t}}</a></li>
      {% endif %}    
     
    </ul>

      
    </div>
  </div>

</div>    

<script type="text/javascript">
  (function($) {
    'use strict';
    $(document).ready(function($) {
      var hashId = window.location.hash;
      if(hashId){
        console.log(hashId);
        $('.switch-form').addClass('hidden');
        $(hashId).removeClass('hidden');
        if (hashId == '#recover'){
          $('.loginForm').removeClass('hidden');
          $('.recoverPassword').addClass('hidden');
          $('.content-form .note').removeClass('hide');
        }
      }
      $('.switch-button').click(function() {
         $('.switch-form').addClass('hidden');
         $($(this).attr('href')).removeClass('hidden');
      });
      
    });
  })(jQuery);
  
</script>